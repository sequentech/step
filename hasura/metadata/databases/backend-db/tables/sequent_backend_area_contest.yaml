table:
  name: area_contest
  schema: sequent_backend
insert_permissions:
  - role: admin-user
    permission:
      check: {}
      columns:
        - annotations
        - labels
        - created_at
        - last_updated_at
        - area_id
        - contest_id
        - election_event_id
        - id
        - tenant_id
    comment: ""
select_permissions:
  - role: admin-user
    permission:
      columns:
        - annotations
        - labels
        - created_at
        - last_updated_at
        - area_id
        - contest_id
        - election_event_id
        - id
        - tenant_id
      filter: {}
      allow_aggregations: true
    comment: ""
  - role: user
    permission:
      columns:
        - annotations
        - labels
        - created_at
        - last_updated_at
        - area_id
        - contest_id
        - election_event_id
        - id
        - tenant_id
      filter:
        _and:
          - tenant_id:
              _eq: X-Hasura-Tenant-Id
          - area_id:
              _eq: X-Hasura-Area-Id
    comment: ""
update_permissions:
  - role: admin-user
    permission:
      columns:
        - annotations
        - labels
        - created_at
        - last_updated_at
        - area_id
        - contest_id
        - election_event_id
        - id
        - tenant_id
      filter: {}
      check: {}
    comment: ""
