{
    "private": true,
    "name": "sequentech",
    "version": "0.0.1",
    "license": "MIT",
    "workspaces": [
      "ui-essentials",
      "test-app"
    ],
    "scripts": {
      "dev": "yarn --cwd ./test-app dev",
      "start": "yarn --cwd ./test-app start",
      "build": "yarn workspaces run build"
    }
  }
  