package docs:adder;

world adder {
    import docs:transactions-manager/transaction;
    export get-manifest: func() -> string;
    export  add: func(x: u32, y: u32) -> string;
    export  add-route: func(data:string) -> string;
}
