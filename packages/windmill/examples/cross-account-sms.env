# Example configuration for cross-account SMS sending
# Copy and modify this file for your production environment

# Basic SMS configuration
SMS_TRANSPORT_NAME=AwsSns
AWS_REGION=us-east-1
AWS_SNS_ATTRIBUTES='{"SenderID": "SEQUENT", "SMSType": "TRANSACTIONAL"}'

# Cross-account configuration
# Set these to enable cross-account SMS sending
AWS_SNS_CROSS_ACCOUNT_ROLE_ARN=arn:aws:iam::123456789012:role/CrossAccountSMSRole
AWS_SNS_CROSS_ACCOUNT_EXTERNAL_ID=your-unique-external-id-here

# Standard AWS credentials (for the current account)
# These can be provided via IAM roles, environment variables, or AWS config files
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Note: When using cross-account access, the SMS configuration (like sender ID,
# spending limits, etc.) should be configured in the target account (123456789012)
# where the CrossAccountSMSRole exists.
