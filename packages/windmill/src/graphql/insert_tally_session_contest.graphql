mutation InsertTallySessionContest(
  $tenantId: uuid!,
  $electionEventId: uuid!,
  $areaId: uuid!,
  $contestId: uuid!,
  $sessionId: integer!,
  $tallySessionId: uuid!
) {
  insert_sequent_backend_tally_session_contest(
    objects:
    {
        tenant_id: $tenantId,
        election_event_id: $electionEventId,
        area_id: $areaId,
        contest_id: $contestId,
        session_id: $sessionId,
        tally_session_id: $tallySessionId!
    }
  ) {
    returning {
      id
      tenant_id
      election_event_id
      area_id
      contest_id
      session_id
      document_id
      created_at
      last_updated_at
      labels
      annotations
      tally_session_id
    }
  }
}