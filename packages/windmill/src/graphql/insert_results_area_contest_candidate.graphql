mutation InsertResultsAreaContestCandidate(
    $tenantId: uuid!,
    $electionEventId: uuid!,
    $electionId: uuid,
    $contestId: uuid,
    $areaId: uuid,
    $candidateId: uuid,
    $resultsEventId: uuid!,
    $castVotes: Int,
    $winningPosition: Int,
    $points: Int,
    $castVotesPercent: numeric,
) {
  insert_sequent_backend_results_area_contest_candidate(
    objects:
    {
        tenant_id: $tenantId,
        election_event_id: $electionEventId,
        election_id: $electionId,
        contest_id: $contestId,
        area_id: $areaId,
        candidate_id: $candidateId,
        results_event_id: $resultsEventId,
        cast_votes: $castVotes,
        winning_position: $winningPosition,
        points: $points,
        cast_votes_percent: $castVotesPercent
    }
  ) {
    returning {
        id
        tenant_id
        election_event_id
        election_id
        contest_id
        area_id
        results_event_id
        cast_votes
        winning_position
        points
        created_at
        last_updated_at
        labels
        annotations
    }
  }
}