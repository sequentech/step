mutation InsertTallySessionExecution(
  $tenantId: uuid!,
  $electionEventId: uuid!,
  $currentMessageId: Int!,
  $tallySessionId: uuid!,
  $documentId: uuid!
) {
  insert_sequent_backend_tally_session_execution(
    objects:
    {
        tenant_id: $tenantId,
        election_event_id: $electionEventId,
        current_message_id: $currentMessageId,
        tally_session_id: $tallySessionId,
        document_id: $documentId
    }
  ) {
    returning {
      id
      tenant_id
      election_event_id
      created_at
      last_updated_at
      labels
      annotations
      current_message_id
      tally_session_id
      document_id
    }
  }
}