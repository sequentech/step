query GetTallySessionHighestBatch(
  $tenantId: uuid!,
  $electionEventId: uuid!
) {
  sequent_backend_tally_session_contest(
    where: {
      election_event_id: {_eq: $electionEventId},
      tenant_id: {_eq: $tenantId}
    },
    order_by: {
      session_id: desc
    },
    limit: 1 
  ) {
    id
    session_id
  }
}