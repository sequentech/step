mutation DeleteLock(
  $key: String!,
  $value: String!,
) {
  delete_sequent_backend_lock (
    where: {
      key: {
        _eq: $key
      },
      value: {
        _eq: $value
      }
    }
  ) {
    affected_rows
  }
}