mutation InsertResultsContestCandidate(
    $tenantId: uuid!,
    $electionEventId: uuid!,
    $electionId: uuid,
    $contestId: uuid,
    $candidateId: uuid,
    $resultsEventId: uuid!,
    $castVotes: Int,
    $castVotesPercent: numeric,
    $winningPosition: Int,
    $points: Int,
) {
  insert_sequent_backend_results_contest_candidate(
    objects:
    {
        tenant_id: $tenantId,
        election_event_id: $electionEventId,
        election_id: $electionId,
        contest_id: $contestId,
        candidate_id: $candidateId,
        results_event_id: $resultsEventId,
        cast_votes: $castVotes,
        cast_votes_percent: $castVotesPercent,
        winning_position: $winningPosition,
        points: $points
    }
  ) {
    returning {
        id
        tenant_id
        election_event_id
        election_id
        contest_id
        results_event_id
        cast_votes
        winning_position
        points
        created_at
        last_updated_at
        labels
        annotations
    }
  }
}