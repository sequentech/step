mutation InsertResultsEvent(
  $tenantId: uuid!,
  $electionEventId: uuid!,
) {
  insert_sequent_backend_results_event(
    objects:
    {
      tenant_id: $tenantId,
      election_event_id: $electionEventId,
    }
  ) {
    returning {
      id
      tenant_id
      election_event_id
      name
      created_at
      last_updated_at
      labels
      annotations
    }
  }
}