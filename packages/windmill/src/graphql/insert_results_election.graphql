mutation InsertResultsElection(
    $tenantId: uuid!,
    $electionEventId: uuid!,
    $resultsEventId: uuid!,
    $electionId: uuid,
    $name: String,
    $elegibleCensus: Int,
    $totalVoters: Int,
    $totalVotersPercent: numeric,
) {
  insert_sequent_backend_results_election(
    objects:
    {
        tenant_id: $tenantId,
        election_event_id: $electionEventId,
        results_event_id: $resultsEventId,
        election_id: $electionId,
        name: $name,
        elegible_census: $elegibleCensus,
        total_voters: $totalVoters,
        total_voters_percent: $totalVotersPercent,
    }
  ) {
    returning {
        id
        tenant_id
        election_event_id
        election_id
        results_event_id
        name
        elegible_census
        total_voters
        created_at
        last_updated_at
        labels
        annotations
        total_voters_percent
    }
  }
}