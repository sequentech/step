query GetAreasByIds(
    $tenantId: uuid!,
    $electionEventId: uuid!,
    $areaIds: [uuid!]!,
) {
  sequent_backend_area(
      where: {
          election_event_id: {_eq: $electionEventId},
          tenant_id: {_eq: $tenantId},
          id: {_in: $areaIds},
      }
  ) {
    id
    name
  }
}