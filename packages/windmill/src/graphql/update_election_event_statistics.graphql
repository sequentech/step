mutation UpdateElectionEventStatistics(
        $electionEventId: uuid!
        $tenantId: uuid!
        $statistics: jsonb!
    ) {
        update_sequent_backend_election_event(
            where: {
                id: {_eq: $electionEventId}
                tenant_id: {_eq: $tenantId}
            }
            _set: {statistics: $statistics}
        ) {
            returning {
                id
            }
        }
    }
