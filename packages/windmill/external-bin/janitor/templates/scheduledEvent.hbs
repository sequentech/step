{
    "id": "{{UUID}}",
    "tenant_id": "{{tenant_id}}",
    "election_event_id": "{{election_event_id}}",
    "created_at": "{{current_timestamp}}",
    "stopped_at": null,
    "archived_at": null,
    "labels": null,
    "annotations": null,
    "event_processor": "{{event_processor}}",
    "cron_config": {
        "cron": null,
        "scheduled_date": "{{scheduled_date}}"
    },
    "event_payload": {
        "election_id": "{{election_id}}"
    },
    "task_id": "tenant_{{tenant_id}}_event_{{UUID}}_{{event_processor}}"
}