import { Canvas, Meta } from '@storybook/blocks'
import * as FooterStories from './Footer.stories'

<Meta of={FooterStories} title="components/Footer"/>

# Footer

A Footer is the box at the top of every screen. It includes the logo, the
software version, language toggle and an optional button to log out.

## Desktop

<Canvas of={FooterStories.Primary} />

## Invalid Translation

The `footer.poweredBy` translation key must contain `<sequent />` to be valid. If it doesn't, an error will be displayed.

<Canvas of={FooterStories.InvalidTranslation} />
