{
  "name": "voting-portal-load-test",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone dependencies for the online-channel load test (test/load), so a load client machine only needs this directory installed rather than the whole Yarn workspace",
  "scripts": {
    "test": "playwright test --config playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
