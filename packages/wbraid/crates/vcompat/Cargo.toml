[package]
name = "vcompat"
version = "0.1.0"
edition = "2021"

# Verificatum wire-format compatibility layer. Deliberately dependency-free:
# this is the layer whose bytes must match VMN exactly, so it should be as small
# and auditable as possible. `hex` is dev-only, for test vectors.

[dependencies]

[dev-dependencies]
hex = "0.4"
