import styled from "@emotion/styled"

export const CustomUrlsStyle = {
    InputWrapper: styled.div`
        width: 100%;
        display: flex;
        flex-direction: row;
        align-items: center;
        gap: 8px;
    `,
}
