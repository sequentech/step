query GetAreas($electionEventId: uuid!) {
        sequent_backend_area(
            where: {election_event_id: {_eq: $electionEventId}}
        ) {
                id
                name
        }
    }