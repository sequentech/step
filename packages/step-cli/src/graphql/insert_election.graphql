mutation InsertElection($tenant_id: uuid!, $election_event_id: uuid!, $name: String!,$external_id: String!, $description: String, $presentation: jsonb) {
  insert_sequent_backend_election(objects: {
    tenant_id: $tenant_id,
    election_event_id: $election_event_id,
    name: $name,
    external_id: $external_id,
    description: $description,
    presentation: $presentation
  }) {
    affected_rows
    returning {
      id
    }
  }
}