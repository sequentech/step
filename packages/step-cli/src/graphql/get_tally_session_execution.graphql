query GetTallySessionExecution($tallySessionId: uuid!) {
  sequent_backend_tally_session_execution(
    where: {
      tally_session_id: {_eq: $tallySessionId}
    }
    order_by: {created_at: desc}
    limit: 1
  ) {
    id
    results_event_id
  }
} 