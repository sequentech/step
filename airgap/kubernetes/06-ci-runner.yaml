apiVersion: v1
kind: ConfigMap
metadata:
  name: runner-config
  namespace: gitea
data:
  config.yaml: |
    runner:
      capacity: 1
      # Use the internal Kubernetes DNS name for portability
      envs:
        KUBERNETES_SERVICE_HOST: kubernetes.default.svc.cluster.local
        KUBERNETES_SERVICE_PORT: "443"
      # Map all standard labels to our custom local image that includes kubectl
      labels:
        - "ubuntu-latest:docker://sequentech.local/runner-ubuntu:22.04"
        - "ubuntu-24.04:docker://sequentech.local/runner-ubuntu:22.04"
        - "ubuntu-22.04:docker://sequentech.local/runner-ubuntu:22.04"
    container:
      workdir_parent: /workspace
      # Allow mounting the service account into build containers
      valid_volumes: ["/var/run/secrets/kubernetes.io/serviceaccount"]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitea-runner
  namespace: gitea
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: gitea-runner
  template:
    metadata:
      labels:
        app: gitea-runner
    spec:
      serviceAccountName: gitea-runner-sa
      initContainers:
        - name: wait-for-token
          image: sequentech.local/runner-ubuntu:22.04
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - "until /usr/local/bin/kubectl get secret gitea-runner-token -n gitea; do echo waiting for secret; sleep 5; done"
      tolerations:
        - key: "node-role.kubernetes.io/master"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: runner
          image: gitea/act_runner:latest
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              until [ -S /var/run/docker.sock ]; do echo 'Waiting for docker socket...'; sleep 2; done
              if [ ! -f /data/.runner ]; then
                echo "Registering runner with Gitea..."
                /sbin/tini -- act_runner register --no-interactive --instance "${GITEA_INSTANCE_URL}" --token "${GITEA_RUNNER_REGISTRATION_TOKEN}" --config /config/config.yaml
                # Ensure the registration file is moved to persistent storage if not already there
                [ -f .runner ] && mv .runner /data/.runner
              fi
              echo "Starting runner daemon..."
              /sbin/tini -- act_runner daemon --config /config/config.yaml
          env:
            - name: GITEA_INSTANCE_URL
              value: http://gitea.gitea:3000
            - name: GITEA_RUNNER_REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: gitea-runner-token
                  key: token
            - name: DOCKER_HOST
              value: unix:///var/run/docker.sock
            - name: CONFIG_FILE
              value: /config/config.yaml
          volumeMounts:
            - name: runner-data
              mountPath: /data
            - name: config-volume
              mountPath: /config
            - name: dind-socket
              mountPath: /var/run
        - name: dind
          image: docker:24-dind
          imagePullPolicy: IfNotPresent
          env:
            - name: DOCKER_TLS_CERTDIR
              value: ""
          securityContext:
            privileged: true
          command: ["sh", "-c", "dockerd-entrypoint.sh dockerd --host=tcp://0.0.0.0:2375 --host=unix:///var/run/docker.sock --insecure-registry gitea.gitea:3000 --default-address-pool base=10.200.0.0/16,size=24 & until docker info >/dev/null 2>&1; do echo 'Waiting for docker...'; sleep 2; done; if [ -d /images ]; then find /images -name \"*.tar\" -exec echo \"Loading {}...\" \\; -exec docker load -i {} \\;; fi; docker tag sequentech.local/runner-ubuntu:22.04 docker.gitea.com/runner-images:ubuntu-22.04 || true; wait"]
          volumeMounts:
            - name: docker-storage
              mountPath: /var/lib/docker
            - name: image-cache
              mountPath: /images
              readOnly: true
            - name: dind-socket
              mountPath: /var/run
            - name: kube-api-access
              mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              readOnly: true
      volumes:
        - name: runner-data
          persistentVolumeClaim:
            claimName: runner-data-pvc
        - name: docker-storage
          persistentVolumeClaim:
            claimName: docker-storage-pvc
        - name: image-cache
          hostPath:
            path: /var/lib/rancher/k3s/agent/images
            type: DirectoryOrCreate
        - name: config-volume
          configMap:
            name: runner-config
        - name: dind-socket
          emptyDir: {}
        - name: kube-api-access
          projected:
            defaultMode: 420
            sources:
              - serviceAccountToken:
                  expirationSeconds: 3607
                  path: token
              - configMap:
                  items:
                    - key: ca.crt
                      path: ca.crt
                  name: kube-root-ca.crt
              - downwardAPI:
                  items:
                    - fieldRef:
                        apiVersion: v1
                        fieldPath: metadata.namespace
                      path: namespace
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: runner-data-pvc
  namespace: gitea
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-storage-pvc
  namespace: gitea
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
